---
interface Props { profile: { name: string; degree: string | null }; }
const { profile } = Astro.props;
---

<footer class="site-footer">
  <span>&copy; {new Date().getFullYear()} {profile.name}</span>
  {profile.degree && <span>{profile.degree}</span>}
</footer>
